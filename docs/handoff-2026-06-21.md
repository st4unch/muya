---
date: 2026-06-21
project: claude-control-plane
type: handoff
---

# Handoff — claude-control-plane — 2026-06-21

## Ne yapıldı (v0.0.5 — bu oturum)
- **3 UI özelliği + 1 bug fix:**
  - **BUG (oturum düşmesi):** Sayfa geçişinde (Sessions/Queue ↔ Control) kontrol paneli unmount olup `<AgentTerminal>` cleanup'ı PTY'yi öldürüyordu. Artık kontrol paneli **her zaman mount**, aktif değilken `hidden` (display:none) — PTY + çalışan `claude` oturumu hayatta kalıyor. (`App.tsx`)
  - **Terminal teması:** dark varsayılan + Sun/Moon toggle, localStorage'da persist, PTY'yi öldürmeden canlı renk değişimi. (`Terminal.tsx`, `App.tsx`)
  - **Branch detayı:** sidebar'da branch'a tıkla → Queue ekranında commit listesi + ahead/behind + değişen dosyalar (gerçek git). (`fs.rs::branch_detail`, `QueuePage.tsx`, `App.tsx`)
  - **Native File menüsü + ⌘N New File:** Apex/File/Edit submenu'leri; New File → save dialog → `create_file` (truncate yok) → editörde aç. (`fs.rs::create_file`, `lib.rs`, `App.tsx`)
- **Security + code review** (paralel subagent) → düzeltildi: branch_detail git arg-injection (tam-nitelikli `refs/heads/` ref + önden `rev-parse --verify`), base-branch'i inceleyince tüm geçmişin "ahead" görünmesi (`ahead=0`), hatalı repo→sessiz boş kart (`Err`), menü listener re-subscribe çift-fire (`workspacesRef`+`[]` deps).
- **Doğrulama:** cargo test 9/9, vitest 14/14, tsc + vite build temiz. İmzalı + **notarize edilmiş** (Accepted, stapled, spctl=Notarized Developer ID) v0.0.5.

## Değişen dosyalar (v0.0.5)
- `src-tauri/src/fs.rs` — `create_file`, `branch_detail` (+ struct'lar, testler)
- `src-tauri/src/lib.rs` — komut kaydı + native menü (setup + on_menu_event → `menu:new-file`)
- `src/App.tsx` — view unmount→hidden, tema state+toggle, menü listener, branch onClick→detay
- `src/components/Terminal.tsx` — theme prop + canlı palet
- `src/components/QueuePage.tsx` — branch detay kartı
- versiyon dosyaları → 0.0.5

## Önceki bağlam (v0.0.3 / v0.0.4)
- **v0.0.3:** PTY çıktı yolu — binary channel (ArrayBuffer) + bounded `sync_channel`+paced flush (backpressure); 9.3GB OOM bug'ı kapatıldı. Echo p95 405ms→3ms.
- **v0.0.4:** 17 ağır komut (git/`claude`/fs) `#[tauri::command(async)]` → main thread'den çıktı. Sayfa-geçişi/typing donmasının kökü.

## Kararlar (gelecek oturumu bağlayan)
- **Stack Tauri'de KALIR — rewrite YOK.** Donma app-code'du, framework değil.
- Bloke eden (git/fs/subprocess) komutlar HER ZAMAN `#[tauri::command(async)]`; PTY/keystroke sync kalır.
- **Uzun-ömürlü/stateful bileşenleri (PTY/xterm) view değişiminde unmount etme** — `hidden` ile gizle (L1).
- `branch_detail` gibi frontend-girdili git komutlarında tam-nitelikli `refs/heads/` ref kullan (option-injection koruması).

## Sıradaki iş
- [ ] **KULLANICI v0.0.5'i gerçek kullanımla doğrulasın** — özellikler derleme/test/review'dan geçti ama çalışan pencerede görsel doğrulanmadı: (1) terminal aç→Queue'ya geç→dön: oturum yaşıyor mu, (2) ⌘N/File>New File, (3) branch→detay, (4) tema toggle.
- [ ] Perf harness (`VITE_APEX_PERF=1`) odaklı pencereyle koşulup baseline ile kıyaslanmalı (CLI'dan pencere odağı verilemiyor → App Nap timer'ları askıya alıyor).

## Dikkat / tuzak
- **Notarization:** keychain profile `apex-notary` (.p8 `~/Downloads/AuthKey_M87Y6CK4GH.p8`). Zip her zaman `ditto -c -k --keepParent` ile (plain `zip` imzayı bozar). Asset adı `ApexMissionControl-<ver>-arm64.zip`.
- Stres dev'i yeniden başlatmadan ÖNCE eski process'i öldür: `pkill -9 -f apex-mission-control; lsof -ti:1420|xargs kill -9`.
- İmzalı .app zip'ini `ditto -x -k` ile aç/doğrula (`unzip` mührü bozar).
