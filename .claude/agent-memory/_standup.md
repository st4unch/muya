# Standup feed (newest on top)

## 2026-08-04
- **[mcp-developer]** `ssh_scp` MCP tool P1 DONE @operator @software-architect — new agent file-transfer tool, AC1-AC7 code+LIVE verified (AC8 real-PSMP + full-app-chain need operator). Operator resolved both of your CANNOT-ANSWERs before I started (see `docs/prd-ssh-scp.progress.md` § Kararlar): sandbox = workspace roots (not a dedicated `~/.claude/muya-scp/` — operator explicitly chose broader-but-already-open workspace confinement over a new sandbox dir), PSMP `-o` = new `PsmpProfile.scpOptions` operator-authored field (matches your CRUX-2 proposal exactly). Built accordingly: `local_guard::resolve_local_scp_path` (canonicalize+prefix+symlink-escape reject against workspace roots, AC3), `broker::enforce_scp_arg_policy` (`-o/-F/-i/-S/-P` hard-denied, `-r/-p/-C/-l` allowed, AC4), `ssh::build_scp_command` (PSMP dest `@`-only, `#` never used, port via `-P`, AC5). **GROUNDING GOTCHA for whoever touches this next:** no Rust-side "workspace roots" source existed (PRD assumed one) — added `workspace_roots.rs` bridge (frontend `localStorage` → `~/.claude/muya-workspace-roots.json` via new `set_workspace_roots` command) since the guardrail is Rust-only. 216 backend tests (+~25), tsc clean, vitest 91/92 (1 pre-existing unrelated fail). Live-verified myself against the existing Docker `muya-ssh-test` container (upload+download+independent `ssh cat`, PASS) — didn't leave this as an operator gap since I had docker access. Detay: agent-memory/mcp-developer/journal.md, docs/prd-verify/step-output-P1.md § "Retry 0 — PRD ssh-scp"
- **[software-architect]** `ssh_scp` MCP tool security model ready @mcp-developer @operator — mirrors ssh_run (alias→resolve→gate→secret-in-Rust→run_with_injection PTY, is_psmp gate unchanged). CRUX-1 (biggest risk = LOCAL disk): scp touches local fs → agent could exfil `~/.ssh/id_rsa` or overwrite files. Confine to a DEDICATED sandbox root `~/.claude/muya-scp/` via canonicalize+prefix-check+symlink-escape reject (NOT workspace roots — too broad). This is NEW code (no local-path validation exists). CRUX-2 (`-o` tension): keep agent `-o` HARD-denied (ProxyCommand/IdentityFile/PKCS11/-S=RCE); Muya-side builds the PSMP-required `-o`+dest from the server's PsmpProfile so agent supplies zero `-o` → tension dissolved. CyberArk scp dest = `vaultUser@targetUser@targetAddr@psmpAddr:/path` (`@`-only; `#` illegal as SCP delim). explicit `direction` enum (never infer). DO NOT BUILD until operator answers 2 CANNOT-ANSWER: (a) exact PSMP `-o` keys their env needs, (b) sandbox root path. Detay: agent-memory/software-architect/journal.md
- **[mcp-developer]** `ssh_run` PSMP 2FA/OTP hardening P1 DONE @operator @software-architect — PSMP RADIUS/OTP challenge prompts (passcode:/verification code:/one-time/otp:) no longer get the stored password injected into them (was an account-lockout risk). `run_with_injection` gained `is_psmp` gate — challenge classification checked BEFORE password-prompt check, PSMP-only, secret withheld + child killed immediately on match; direct-SSH untouched (regression test proves `passcode:` still injects on direct). broker `handle_run`: challenge → `err_resp` pointing at ssh_open (AC2); PSMP timeout with no prompt seen → additive `"message"` RADIUS-push hint (AC3). `ssh_run` tool description documents the limit (AC5). 191 backend (+3), `cargo check` both bins clean. **GAP — needs operator + real PSMP access:** AC1 (real-PSMP live plain-password run) and AC6 (full live e2e) NOT run this session; local `sh`-based PTY harness (no Docker/PSMP) proves the withhold/kill mechanics + no-regression but not real PSMP's actual layered-prompt sequence. Detay: agent-memory/mcp-developer/journal.md, docs/prd-verify/step-output-P1.md

## 2026-08-01
- **[mcp-developer]** Agentic SSH `ssh_add_server` DONE @software-architect @operator — agents register an SSH server then ssh_open/ssh_run it. `Server.agent_added` flag; core `agent_add_server_in` forces direct + ssh_options=None, agent_access+agent_added=true, CREATE-ONLY (can't overwrite human servers), rejects empty/whitespace/control/`@` in host+user (argv/user@host injection). broker `add_server` op + proxy `ssh_add_server` tool + amber "agent-added" UI badge. **OPERATOR OVERRODE PRD D1**: agent MAY attach a stored credential BY NAME (personal-desktop, accepted risk) — value resolved+injected Rust-side via `secret_for_ref` (also swapped ssh_pty_connect local branch id-only→name-or-id), never seen by agent; all OTHER guardrails kept. 185 backend(+6)/88 vitest/tsc/both bins build; proxy tools/list live-shows ssh_add_server + app-down⇒-32000. GAP: add_server not smoke-tested through live app UDS (GUI runtime). Detay: agent-memory/mcp-developer/journal.md
- **[software-architect]** `ssh_add_server` agentic-SSH design ready @mcp-developer @operator — mini-PRD `docs/prd-ssh-agent-add-server.md`, AWAITING OPERATOR on D1 credential boundary. CRUX (D1, one-way door): agent-added servers FORCED `credentialSource=prompt` in Rust — agent adds host+user, NEVER auto-attaches a stored/CyberArk secret (attaching = human-only UI). Kills the confused-deputy exfil (add host=evil + stored prod-pass → inject into fake sshd). New `agentAdded` flag. D2: expose only `ssh_add_server` (+ `ssh_remove` gated to agentAdded); NO sshOptions to agent (`-o ProxyCommand=` = local RCE), NO psmp, NO set-credential tool. D4: reuse `upsert_server_in` create-only dedup (agent can't overwrite human servers). Do NOT build until operator answers D1. Detay: agent-memory/software-architect/journal.md

## 2026-07-26
- **[mcp-developer]** SSH Agent Broker Faz 3.1 (AC11–AC16) DONE: secret-operation ENGINE + EMPTY registry. NEW `agent_ops.rs` — env-injected fixed-program ops (aws/kubectl/git USE a stored secret, never read it); agent references op BY NAME, secret goes into CHILD process env only. CRUX = `enforce_arg_policy` PURE fail-closed (reject leading-`-` positional, hard-denylist `-c/--endpoint-url/--kubeconfig/--output/-o/--query`+op deny, unknown flags, word-shaped non-pinned subcommands; flag values MUST be `--flag=value`). `execute_op` = std::process::Command argv-only env_clear+envs, NO shell, 256KiB cap. credstore +`description`+`token` kind+`list_meta`. broker +3 ops, proxy +3 MCP tools (list_secrets/list_operations/run_operation). Registry `~/.claude/muya-agent-ops.json` operator-only (agents can't register). 168 backend(+16)/86 vitest/tsc/proxy tools/list live-verified. @software-architect — engine done, EMPTY registry by design; GAP: run_operation not smoke-tested through live app UDS (needs GUI runtime) — recommend before Faz 3.2 real ops; `.app` 2nd-binary bundling still open. Detay: agent-memory/mcp-developer/journal.md
- **[mcp-developer]** SSH Agent Broker Faz 2 (AC8–AC10) DONE: `ssh_run(alias, command)` — one remote command, stdout captured, password injected app-side/never exposed. New `pty::run_with_injection` (real PTY, 256KB bounded buffer, timeout+kill, prompt-line stripped); broker `run` op (async `handle_run`, resolve+gate+secret, prompt-source rejected); tokio Semaphore N=4 concurrency cap; proxy `ssh_run` MCP tool. 152 backend (+4)/proxy builds; live e2e `run_with_injection_live` PASS (Docker sshd → MUYA_RUN_OK, exit 0, no pw). @software-architect — SSH broker Faz 1+2 complete; only open item: `.app` release bundling of muya-ssh-mcp 2nd binary (tauri externalBin). Detay: agent-memory/mcp-developer/journal.md
- **[mcp-developer]** SSH Agent Broker Faz 1 (AC1–AC7) DONE: new `muya-ssh` MCP server — agents SSH by alias, password stays Rust-side. `broker.rs` owner-only UDS (0600 + `libc::getpeereid` uid check, SEPARATE from chat bridge) + `src/bin/muya_ssh_mcp.rs` stdio proxy (tools ssh_list_servers/ssh_open, NOT ssh_run). 148 backend/82 vitest/tsc green, live e2e 18/18. @software-architect — Faz 2 `ssh_run` (AC8–10) out of scope/not started; `.app` bundling of 2nd binary needs tauri externalBin for release. Detay: agent-memory/mcp-developer/journal.md

## 2026-07-17
- **[mcp-developer]** Remote data channel wired end-to-end: bridge_remote_listen accept loop now enqueues into the SAME BridgeState broker queue local UDS uses (TLS-verified SPKI, not self-reported peer field); new bridge_remote_send command dials over mutual-auth TLS 1.3 via new PinnedServerCertVerifier (NOT NoCertVerifier). 4 new real-socket tests, 102/102 backend + 55/55 frontend pass. @software-architect — bridge_remote_listen has NO frontend UI call site yet (pairing works, listener never starts) — detay: agent-memory/mcp-developer/journal.md

## 2026-07-15
- **[mcp-developer]** Faz 3 task-execution DONE: bridge_exec.rs — sandboxed exec (TempDir cwd + deny-list env strip), bounded streaming channel(256), capability scope gate, auto-run three-path, memory-only audit log, fan-out independence, AC-3-7 shell two-factor override. 32 new tests; 96/96 pass. Next: Faz 4 (OS sandbox) or frontend bridge UI @software-architect — detay: agent-memory/mcp-developer/journal.md
- **[mcp-developer]** Faz 2 remote-mtls DONE: bridge_remote.rs — Ed25519 identity, SPAKE2 PAKE pairing, PinnedSpkiVerifier (fail-closed mTLS), PeerRegistry, PIN lifecycle (TTL/lockout/single-use), 6 Tauri commands. 23 new tests, 62/62 total. CPace→SPAKE2 substitution documented (ADR R7). Next: Faz 3 task handoff @software-architect — detay: agent-memory/mcp-developer/journal.md
- **[mcp-developer]** Faz 1 local-mvp DONE: bridge.rs accept loop + framing + broker queue + ApprovalState gate + 3 Tauri commands. 9/9 tests pass. Next: Faz 2 (TCP+TLS+CPace) @software-architect — detay: agent-memory/mcp-developer/journal.md

## 2026-07-15
- **[software-architect]** ADR 0002 Claude-Remote-Bridge kararları hazır @mcp-developer @inference-developer @devops-developer — seam: ayrı local(UDS)/remote(mTLS TCP) socket, CPace pairing, broker-queue handoff (RAW PTY WRITE YASAK), approve-each default. 5 CANNOT-ANSWER operatörü bekliyor. Detay: agent-memory/software-architect/journal.md + docs/adr/claude-remote-bridge-architecture.md

## 2026-07-24
- **[software-architect]** ssh-cyberark PRD re-gate round 3 = NO-GO (1 fix) @operator @mcp-developer — R14 SSH key temp-file path contradicts §9 "no plaintext secret on disk" (crash leaves key on disk; CyberArk-brokered keys also hit disk). REQUIRE ssh-agent-only (agent protocol, zero disk write); rewrite AC2.4. All other fixes (ARCH1/2, PM1-4, D8/D9 RADIUS) verified coherent. Detay: agent-memory/software-architect/journal.md
- **[software-architect]** ssh-cyberark PRD = NO-GO (2 small binary fixes): keyring crate can't do Touch-ID/ThisDeviceOnly → use security-framework (D4/AC0.5); cyberark reveal→JS leak missing from Risk Register. O2 caps NOT a blocker (Rust reqwest+pty bypass frontend gate). Cites 8/8 verified. Detay: agent-memory/software-architect/journal.md

## 2026-07-26
- **[software-architect]** agent-ops design ready @mcp-developer — env-injected fixed-program tools (aws/kubectl/git USE secret, never read it). CRUX (D6): threat isn't `echo $KEY`, it's argument-injection escape hatches (`git -c core.sshCommand`, `aws --endpoint-url`, `-o/--output` writes). Mitigation = per-op argv allowlist + pinned subcommand + flag denylist, NOT arbitrary args. `handle_run` already in broker.rs:278. Detay: agent-memory/software-architect/journal.md
